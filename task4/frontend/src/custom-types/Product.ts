export type Product = {
    sku: string,
    name: string,
    description: string,
    unit_price: number,
    unit_weight: number,
    category_code: string,
    category_name: string,
    quantity: number,
}